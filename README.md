**Bibliotech** è una **web app full-stack** basata su **Next.js** (frontend) e **Django REST Framework** (backend).  
Permette di gestire in maniera digitalizzata una Bibliotca e consolidare lo stack tecnico usato — con autenticazione, creazione, modifica e ricerca di libri , creazione tessere, prestiti ecc.. — tramite **API RESTful** sicure e performanti.

---

## 🚀 Obiettivi del progetto
Bibliotech nasce come esercizio pratico per:
- Approfondire l’integrazione **Next.js ↔ Django REST API**
- Gestire dati reali tramite **ORM Django + chiamate asincrone Next.js**
- Strutturare un’architettura **scalabile e pulita**
- Lavorare con **MSSQL** come database enterprise

---

## 🧠 Funzionalità principali
- 🔐 **Autenticazione JWT** (login, logout)
- 📚 **CRUD completo** per libri e categorie
- 🔍 **Filtraggio dinamico** (per titolo, autore, categoria)
- ⚡ **Client-side fetching** con `fetch()` / `axios`
- 🌗 UI reattiva costruita con **React + Next.js**
- 🔄 API REST sviluppate con **Django REST Framework**
- 🧱 Database relazionale (MSSQL)
- 🧩 Routing automatico e gestione stato client-side

---

## 🧰 Stack Tecnologico

| Livello | Tecnologia | Descrizione |
|----------|-------------|-------------|
| **Frontend** | Next.js (React, Axios, TailwindCSS) | Rendering , UI moderna e performante , centralizzazione e destrutturizzazione degli state redux, interceptor apiClient axios |
| **Backend** | Django + Django REST Framework | Gestione dati, autenticazione JWT, logica server |
| **Database** | MSSQL | Persistenza dati relazionale |
| **API** | RESTful (JSON) | Comunicazione tra front-end e back-end |
| **Auth** | JWT | Token-based authentication |
| **Tools** | Git, GitHub, Postman | Versionamento, test e documentazione API |

---
